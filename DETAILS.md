# bkbook — 詳細

[README.md](README.md) は TUI までで、ここから先はそれ以外の全部である。
コマンドライン、ライブラリとしての使い方、外字、対応している形式、
そして libeb のどのソースをどこへ移したか。

[コマンドライン](#コマンドライン) ·
[本文の検索](#見出し語ではなく本文を検索する) ·
[欄を埋めて検索する](#欄を埋めて検索する) ·
[本自身の目次](#本自身の目次) ·
[見出し語の末尾で探す](#見出し語の末尾で探す) ·
[ライブラリとして](#ライブラリとして) ·
[外字](#外字) ·
[対応している形式](#対応している形式) ·
[参照実装](#参照実装)

## コマンドライン

```console
$ python -m bkbook info   BOOK                # 本の中身を見る
$ python -m bkbook search BOOK WORD           # 一致する見出し語を並べる
$ python -m bkbook show   BOOK WORD           # 項目を全文で出す
$ python -m bkbook entry  BOOK PAGE:OFFSET    # 位置を指定して項目を出す
$ python -m bkbook keyword BOOK WORD...       # その語を *使っている* 項目
$ python -m bkbook multi  BOOK [WORD...]      # ラベル付きの欄で検索する
$ python -m bkbook menu   BOOK [PAGE:OFFSET]  # 本自身の目次
$ python -m bkbook copyright BOOK             # 出版社の権利表示
$ python -m bkbook gaiji  BOOK [CODE...]      # 外字のビットマップを見る
$ python -m bkbook tui    BOOK...             # 対話インタフェース
$ python -m bkbook list   BOOK...             # 開かれる辞書を順に並べる
$ python -m bkbook stopcode BOOK              # 項目の終わりを示すエスケープ
```

`list` は辞書ごとに形式と、見つかった appendix を、TUI が開く順に表示する。
`list --order` は同じ内容を順序ファイルの形で出力する。編集して
`~/.config/bkbook/books.config` に保存すればよい（利用者ごとの設定なので、
リポジトリには置かない）。

`BOOK` は `catalog`（EB）または `catalogs`（EPWING）を含むディレクトリ、
またはそれらを収めたディレクトリ。パスは必ず引数で渡す。

よく使うオプション:

| オプション | 意味 |
|---|---|
| `-s`, `--subbook` | サブブックを番号・ディレクトリ名・タイトルで選ぶ |
| `-n`, `--limit` | 結果の最大件数 |
| `-e`, `--exact` | 前方一致ではなく見出し語全体の一致 |
| `-f`, `--full` | 一覧ではなく項目本文を出す（`keyword`、`multi`） |
| `-a`, `--appendix` | appendix（`*.app`）のファイルまたはディレクトリ——下記参照 |
| `-g`, `--gaiji` | 組み込み表に重ねる外字対応表ファイル |
| `--raw` | 全角文字を ASCII に畳まずそのまま出す |

項目本文はすべて JIS X 0208 で入っているので、英語まで
`ｆｕｌｌ－ｗｉｄｔｈ` と出てくる。CLI はそれを ASCII に畳み戻し、
一部のディスクが本文中にふつうの文字として持っている音声・画像の印を落とす。
ここにはそれを鳴らす手段も描く手段もない。`--raw` を付ければ全部そのまま残る。

この畳み込みは意図して NFKC より狭くしてある。NFKC は、辞書が項目の構造を
示すのに使っている囲み文字まで分解してしまい、広辞苑 の下位語義の印 ㋐㋑ を
裸の ア と イ にしてしまう。

研究社新和英大辞典 はローマ字表記のアクセントを罫線素片 2 つで書く——
上がるところが `┏`、下がるところが `┓`——が、これは語の高い部分の上に
引かれていた線の残りである。線は元に戻す。`a┓me`（雨）は `a̅me`、
`a┏me`（飴）は `am̅e̅` と読める。

## 見出し語ではなく本文を検索する

ディスクによっては、もう 1 種類の索引を持っている。見出し語ではなく
**本文中の語** の索引で、その語を使っているすべての項目を指している。
語を複数並べれば AND になる。

```console
$ python -m bkbook keyword eb/wadai BLACK SHEEP -n 3
  1. はじさらし 恥晒し    [121901:1232]
  2. こまりもの 困り者    [130232:1010]
  3. ¶彼は一家の困り者です. He is the black sheep of the family.    [130232:1744]
```

このどれも、どちらの語の下にも収められていない。索引されているのは訳文中の
英語なので、和英辞典を逆から引けることになる。研究社新和英大辞典、
三省堂の英和・和英、角川類語新辞典、ことわざ・名言の泉 がこの索引を持ち、
類語 はもう 1 つ持っている（`--index cross`）。TUI では `^K` で
同じ問い合わせをこちら側に投げられる。

## 欄を埋めて検索する

いくつかのディスクは、一度に 2 つ以上のことを尋ねてくる。
ことわざ・名言の泉 は 事項 **と** 作者 を求めるが、その両方に答えるのは
どの索引 1 つにも頼めない検索である:

```console
$ python -m bkbook multi eb/kotowaza            # 何を尋ねてくるか
0. 事項　Ｆ１で選べます / 作者・作品　Ｆ１で選べます
     0. 事項　Ｆ１で選べます
     1. 作者・作品　Ｆ１で選べます

$ python -m bkbook multi eb/kotowaza こい さいかく -n 2
  1. 恋といふくせもの、諸人心を【和】    [1066:586]
  2. 人間と生を請けて、女の男只【和】    [2505:1518]
```

`-` はその欄を問わないという意味、`-m` は multi 検索が複数あるときに選ぶ。
日本大百科全書 は 2 つ持っており（索引語 と 図版コード）、三省堂の英和・和英
は 1 つ持っていて、そちらはカタカナで書いた発音を受け取る。

欄によっては、何に答えられるかのメニューを持っていることがある。木構造に
なっていて、その葉が語そのものである:

```console
$ python -m bkbook multi eb/kotowaza -c 0
  1. 人生・人の生死・老い・若さ    [5202:436]
  …
$ python -m bkbook multi eb/kotowaza -c 0 --at 5204:1828
  1. 人生    (a word)
  2. 一生    (a word)
```

## 本自身の目次

```console
$ python -m bkbook menu eb/kotowaza
  1. ことわざ事項分類    [3702:234]
  2. 日本名言事項分類    [3702:668]
  …

$ python -m bkbook menu eb/kotowaza 3702:234
  1. 人生・人の生死・老い・若さ    [3703:680]
  …
```

どの行にも行き先が書かれているので、`menu` で下りていき、着いたところを
`entry` で開ける。`copyright` はどのディスクにもある権利表示を出す。

## 見出し語の末尾で探す

語をそのまま書けば、その語で **始まる** 見出し語に一致する。前に `*` を
付ければ、その語で **終わる** ものに一致する:

```console
$ python -m bkbook search eb/kojien '*ぎらい' -n 4
  1. ぎらい【儀礼】    [4757:1240]
  2. ごしょう‐ぎらい【後生嫌い】(‥シヤウギラヒ)    [6418:1578]
  3. け‐ぎらい【毛嫌い】(‥ギラヒ)    [5514:1906]
  4. まけ‐ぎらい【負け嫌い】(‥ギラヒ)    [16542:894]
```

TUI で打っているときも同じように使える。

これは見出し語一覧の走査ではない。ディスクは、すべての見出し語を **逆順** に
収めた第 2 の索引を持っている。だから「で始まる」に答えるのと同じ B 木が
「で終わる」にも答えられる。問い合わせを引く前に逆順にするだけでよい。
これが本物の索引であることから、2 つの帰結が出てくる:

* すべてのディスクが持っているわけではない。リーダーズ＋プラス、ロングマン、
  ことわざ・名言の泉 は持っておらず、`*` 検索では失敗するのではなく単に
  結果を出さない。
* ある語尾の下に何を収めるかは出版社の判断であって、文字列一致ではない。
  ランダムハウス は `*ization` に `Ju・da・ize` でも答える。*Judaization* を
  引く読者が着地すべき場所がそこだからである。

`*word*` は部分一致検索になる。手元のディスクにはそれ用に鍵づけられた索引が
1 つもない——見出し語は先頭か末尾で収められていて、途中で収められることは
ない——ので、黙って別の答えを返すのではなく拒否する。末尾の `*` だけなら
既定の動作という意味で、そのまま通る。

## ライブラリとして

```python
from bkbook import Book
from bkbook.search import search
from bkbook.text import read_heading, read_text

book = Book("/path/to/dictionary")
subbook = book.subbooks[0]

for hit in search(subbook, "book", limit=5):
    print(read_heading(subbook, hit.heading).text)

hit = search(subbook, "book", limit=1)[0]
entry = read_text(subbook, hit.text)
print(entry.text)
for reference in entry.references:
    print(reference.text_of(entry.text), "->", reference.position)
```

整形を制御したければ `bkbook.text.Renderer` を継承して `read_text` に渡す。
できあがった文字列ではなく、文字・改行・外字・強調範囲・参照のそれぞれについて
呼び出しが来る。

## 外字

本が必要としているのに JIS X 0208 にない文字——発音記号、合字、ロゴ——は、
ディスク固有の符号のもとにビットマップとして収められている。符号はその本の
外では何の意味も持たないので、Unicode への対応表は本ごとに用意する。

対応のない文字は `<gaiji=z1234>` として出力する。目に見えるし grep もできる。
埋めるには、まずビットマップを見る:

```console
$ python -m bkbook gaiji BOOK ha14c
ha14c  →  'ə'
········
········
········
········
········
········
·████···
█····█··
······█·
······█·
███████·
█·····█·
·█···█··
··███···
```

そして対応表ファイルを書き、`-g` で渡す:

```
# 1 行 1 対応: 符号、空白、置き換え
# h = 半角、z = 全角
ha14c   ə
ha177   ː
zb121   —
ha15c           # 置き換えが空なら何も出力しない
```

`python -m bkbook gaiji BOOK -u` は、まだ対応のない符号だけを並べる。

これを書くにあたって相手にしたコレクション、その全辞書分の対応表は
`bkbook/gaiji.py` に同梱されていて、サブブックのタイトルで照合して自動的に
適用される。ほとんどは実際に出現する外字を全部覆っており、どの本でも
出現の 95% を下回るものはない。

対応表は **東アジア曖昧幅** の記号を避けている。この幅の文字は端末によって
1 桁にも 2 桁にもなり、与えられた 1 桁に全角で描くフォントは隣の文字の上に
重ねて書いてしまう。だから 広辞苑 の語義番号は `①②` ではなく `(1)(2)`、
可算・不可算は `ⒸⓊ` ではなく `(C)(U)`、矢印は `->` `<->` `=>` として出る。
文字（letter）は例外である。`ə ð á ŋ θ` は発音そのものであり、ASCII の
代用表記（`#e`、`dh`）こそ、この対応表が改善するために存在するものだからだ。

### どうやってこの表を起こしたか

ビットマップを目で読んで起こした。先に自動化のほうを測って、効かないことが
分かったからである。

**ピクセル照合は上限が低い。** Noto Serif JP / Noto Sans JP と相互に測って
top-1 55〜60%、top-10 でも 63% で頭打ちだった。正規化を 3 通り（インク bbox の
正方化 / em ボックス / 参照字形で較正）試しても変わらない。実データでも
穭（za46d）は 1 位 櫓、2 位 穐、3 位 穭 で、木偏と禾偏の差はピクセル距離に
出ない。

**辞書をまたいだ転移も効かない。** appendix のある 7 冊で 1 冊を隠し、残りの
ラベル付き字形から最近傍を引く leave-one-out で、top-1 は 17%、完全一致に
至っては 0% である。同じ ə が辞書間で 4/128 ビットしか違わないのに当たらない
のは、識別すべき情報（アクセントの傾き）とフォント間のノイズが同じ大きさ
だからで、最近傍は「似た別の字」を引く。

**効くのは字形と文脈の組み合わせである。** 48px のビットマップを 4 倍の
最近傍で拡大し、符号のラベルを付けて 40〜60 個ずつコンタクトシートに並べ、
その字が実際に出てくる本文と並べて読む。これで 12/12 正解した。字形だけでは
「季」か「李」か怪しくても、直後に「冬」があれば決まる。発音記号なら、発音の
分かっている語を引いて連立方程式のように解く——`think [?ɪ́ŋk]` の `?` は θ、
`book [b?k]` は ʊ か ú。辞書内部の一貫性も決め手になる。chujiten の `0xa13a`
は最初 ɑ́ と見たが、father・house・light が同じ字形を共有していると気づいて
á に訂正した（IPA なら別字のはずで、この辞書は研究社式ですべて plain a
だった）。

**並びに構造があれば一気に埋まる。** ランダムハウス英語辞典 は `0xa269` から
Latin-1 が連番で並んでいた——À から ÿ まで 62 文字、2 セルにまたがる Æ/æ だけ
別の場所にある。`gaiji.run()` で一括生成できる。ただしこの配置は他の 5 冊には
なく、そちらは基底文字ごとにアクセント変種をまとめている。

手順は 3 つ。本文を頭から末尾まで歩いて実際に出現する符号と文脈を集め、
コンタクトシートに描き、`gaiji.py` に書く。シートを描くのに PIL は要らない
——`zlib` と `struct` で 8bit グレースケールの PNG を書き、ラベル用に 5x7 の
フォントを 18 字（0-9a-f h z）持てば足りる。実際に出現する外字は 1 冊あたり
数十種しかないので、1 冊 30 分ほどで終わる。

### appendix ファイル

Motoyuki Kasahara 氏が libeb 用に公開した appendix は、日本の CD-ROM 辞書の
ほとんどを覆っている。bkbook はその `*.app` のソース形式をそのまま読む:

```console
$ python -m bkbook show BOOK word -a ~/eb/appendix
```

`-a` には `.app` ファイル、その本の appendix ディレクトリ、コレクション全体の
appendix を収めたディレクトリのいずれも渡せる。

appendix ディレクトリの中では、ファイル名は **サブブック** のディレクトリ名で
つく——`PLUS` にあるサブブックなら `plus.app`。この名前は無関係な辞書どうしで
重複するので、コレクションの中では appendix ディレクトリのほうを **本** の
ディレクトリ名で照合する。`eb/genius` は `eb/appendix/genius` と、`eb/genius2`
は `eb/appendix/genius2` と対になる。サブブック名だけでは決して区別できない
——どちらも自分を `GENIUS` と名乗るからだ。

appendix が与えてくれるものは 2 つ。本が定義する文字すべての置き換えテキストと、
項目の終わりを示すエスケープシーケンスである。bkbook はどちらも自力で割り出す
——上に書いた組み込み表と、下に書く stop code——ので、2026 年 8 月時点で
どちらも必要としていない。appendix のあるコレクション内の全 7 辞書を端から端まで
読んだうえで、appendix が与えていて組み込み表が与えていない文字の数は
**ゼロ** である。

それでも対応を残してあるのは、これらのファイルこそ答えの出どころだったから
であり、また他人のディスクが bkbook の見たことのないものかもしれないからである。
appendix の置き換えは設計上 ASCII（á が `'a`、ə が `#e`、θ が `O-`）なので、
下地として使い、その上に組み込みの Unicode 表を重ねる。bkbook のほうが良く
知っているところは `#e` ではなく `ə` になり、それ以外の場所でも読める形で出る。

### 項目はどこで終わるか

本文ファイルは 1 本の長い流れである。項目と項目のあいだにあるのはエスケープ
だけで、どのエスケープを選んだかはディスクのどこにも書かれていない。libeb は
それを appendix から読み、appendix がなければ「項目を開いた keyword エスケープ
が再び現れたところで終わる」と推測していた。この推測はよく当たるが、記事を
途中で切ることもある。ジーニアス英和大辞典 は *absent* の 1,369 文字を
これで失う。

しかし本は知っていて、遠回しに教えてくれる。索引はすべての項目の開始位置を
持っているので、ある項目の直前のバイトは、その 1 つ前の項目を終わらせたバイト
である。数百項目を抜き出して先頭の並びを見ると、どれにも共通するエスケープが
2 つ見つかる。字下げと keyword。どちらでも本文は区切れるので、順に試す——
それで項目を読んでみて、次の項目の先頭に着地するか、そこから連鎖できるかを
見る——記事の途中で止まらないほうが勝つ。両方とも完璧なら、境界に近いほうを
採る。appendix が記録しているのもその選択である。

```console
$ python -m bkbook stopcode eb/dhk01
[0] 電子ブック版『日本大百科全書』
   -> 0x1f41 0x0090  opens 100.0% of entries at +4, reads 100.0% of them to the next one
      0x1f09 0x0001  opens 100.0% of entries at +0, reads  96.1% of them to the next one
      appendix says 0x1f41 0x0090 — agrees
```

これが厄介なほうの例である。日本大百科全書 は図版のキャプションを記事の
書き出しと同じ字下げで組むので、前のエスケープを使うと 25 記事に 1 つが
キャプションで終わってしまい、その後ろの keyword が本当の境界になる。
三省堂「大辞林」 は外から見ると同じ形をしているのに、逆の結論になる。
あちらでは内側の字下げが 子見出し を開いていて、それ自体が独立した項目
だからである。

答えはサブブックごとに 1 度、最初に項目を読むときに求められ、0.5 秒ほど
かかる。このコレクションについて公開されている 8 つの stop code すべてと
一致しており、それが回帰テストになっている——`StopCodeAgreementTest` が
その 1 つ 1 つをディスクから導き直す。

## 対応している形式

| | |
|---|---|
| 配置 | EB（`catalog`）、EPWING（`catalogs`）——どちらも実物のディスクで検証済み |
| 圧縮 | 無圧縮、ebzip（`*.ebz`）、EPWUTIL で squeeze したディスクを含む |
| 検索 | word（前方一致）、endword（後方一致）、exactword、keyword と cross、multi、menu と copyright、alphabet / kana / as-is の各索引 |
| 本文 | エスケープシーケンスの完全な解釈、参照、見出し |
| フォント | 半角・全角のビットマップ。EB は本体内、EPWING は `gaiji/` ファイル、16/24/30/48px |
| appendix | Motoyuki Kasahara 氏の `*.app`——外字テキストと項目の stop code。任意。どちらもディスクから割り出せる |

**対象外**: EPWING 圧縮（`honmon2`）、S-EBXA、埋め込みの画像と音声。これは
libeb の移植ではない。手元の 1 棚分のディスクを読むために存在するもので、
その棚にはこれらの形式のディスクが 1 枚もない。ここにあるコピーは無圧縮か
ebzip で、squeeze したものは作成時に画像と音声が削除されている。

## 参照実装

libeb 4.4.3。

```
curl -sSL -o eb-4.4.3.tar.bz2 https://github.com/mistydemeo/eb/releases/download/v4.4.3/eb-4.4.3.tar.bz2
tar xjf eb-4.4.3.tar.bz2
```

対応: `zio.c`→`zio.py` / `book.c`→`book.py` / `subbook.c`→`subbook.py` /
`setword.c`+`word.c`→`setword.py` / `match.c`→`match.py` /
`search.c`+`exactword.c`→`search.py` / `readtext.c`→`text.py` /
`narwfont.c`+`widefont.c`→`font.py`
